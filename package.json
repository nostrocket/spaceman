{
  "name": "missioncontrol",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^4.3.2"
  },
  "dependencies": {
    "nostr-tools": "^1.10.1",
    "showdown": "^2.1.0"
  }
}
